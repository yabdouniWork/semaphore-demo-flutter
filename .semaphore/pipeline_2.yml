version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: Build android APK
          commands:
            - flutter build apk --release
            - bundle exec fastlane deploy
